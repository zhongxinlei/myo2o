<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imooc.myo2o.dao.UserLoginMapper">
    <select id="queryAll" resultType="com.imooc.myo2o.entity.UserLogin">
        select * from userLogin
    </select>

    <insert id="add" parameterType="com.imooc.myo2o.entity.UserLogin">
        insert into userLogin values (#{username},#{password})
    </insert>

    <select id="queryByName" resultType="com.imooc.myo2o.entity.UserLogin">
        select * from userLogin where username = #{username}
    </select>

</mapper>
